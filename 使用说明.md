# 农产品质量安全追溯与分析系统 - 使用说明

## 一、环境要求

### 只需要安装两个软件：

1. **Docker Desktop** （必须）
   - 下载：https://www.docker.com/products/docker-desktop
   - 安装后重启电脑
   - 启动 Docker Desktop，等待托盘图标变绿

2. **Python 3.8+** （必须）
   - 下载：https://www.python.org/downloads/
   - 安装时勾选 "Add Python to PATH"

> ⚠️ **不需要** 安装 IDEA、MySQL、Hadoop、Hive、Spark 等软件！
> 所有大数据组件都通过 Docker 容器运行。

### 硬件要求
- 内存：建议 8GB 以上（Docker 容器较多）
- 硬盘：至少 15GB 可用空间
- CPU：建议 4 核以上

---

## 二、快速启动

### 方式一：双击启动（推荐）

1. 确保 Docker Desktop 已启动（托盘图标为绿色）
2. 双击 `启动.bat`
3. 等待约 2 分钟，系统会自动：
   - 构建 Hive 镜像（首次需要下载，约 5 分钟）
   - 启动所有 Docker 容器
   - 安装 Python 依赖
   - 启动 Flask Web 应用
4. 浏览器访问：http://localhost:5000

### 方式二：手动启动

```bash
# 1. 启动Docker容器（首次会自动构建镜像）
docker-compose up -d --build

# 2. 等待90秒让服务完全启动
timeout /t 90

# 3. 安装Python依赖
cd flask_app
pip install -r requirements.txt

# 4. 启动Flask应用
python app.py
```

### 停止系统

- 双击 `停止.bat`
- 或执行：`docker-compose down`

---

## 三、系统架构

```
┌─────────────────────────────────────────────────────────┐
│                    用户浏览器                            │
│                 http://localhost:5000                   │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│                  Flask Web 应用                         │
│                   (本地 Python)                         │
└─────────────────────────────────────────────────────────┘
                           │
          ┌────────────────┼────────────────┐
          ▼                ▼                ▼
┌─────────────────┐ ┌─────────────┐ ┌─────────────────┐
│   HiveServer2   │ │   MySQL     │ │  Spark Master   │
│  (端口 10000)   │ │ (端口 3307) │ │  (端口 8080)    │
└─────────────────┘ └─────────────┘ └─────────────────┘
          │                               │
          ▼                               ▼
┌─────────────────┐               ┌─────────────────┐
│ Hive Metastore  │               │  Spark Worker   │
│  (端口 9083)    │               │  (端口 8081)    │
└─────────────────┘               └─────────────────┘
          │
          ▼
┌─────────────────────────────────────────────────────────┐
│                    Hadoop HDFS                          │
│         NameNode (9870) + DataNode (9864)              │
└─────────────────────────────────────────────────────────┘
```

---

## 四、Docker 服务说明

| 服务 | 容器名 | 端口 | 用途 |
|------|--------|------|------|
| MySQL | hive-mysql | 3307 | Hive 元数据存储 |
| NameNode | namenode | 9870, 9000 | HDFS 主节点 |
| DataNode | datanode | 9864 | HDFS 数据节点 |
| Hive Metastore | hive-metastore | 9083 | Hive 元数据服务 |
| HiveServer2 | hive-server | 10000, 10002 | Hive 查询服务 |
| Spark Master | spark-master | 8080, 7077 | Spark 主节点 |
| Spark Worker | spark-worker | 8081 | Spark 工作节点 |

### 查看服务状态
```bash
docker ps
```

### 查看服务日志
```bash
docker logs hive-server
docker logs namenode
```

---

## 五、系统功能

### 5.1 首页 (http://localhost:5000)
- 系统入口，数据概览
- 快速追溯码查询

### 5.2 消费者查询 (/consumer)
- 输入追溯码查询产品全流程
- 展示种植、加工、运输、销售信息
- 运输温度监控图表

### 5.3 企业管理 (/enterprise)
- 产品列表管理
- 追溯码生成
- 各环节数据录入

### 5.4 监管部门 (/supervisor)
- 监管统计数据
- 风险事件处理
- 质量趋势分析

### 5.5 数据统计 (/statistics)
- 数据概览仪表盘
- 各类统计图表
- 企业质量排行

### 5.6 风险预警 (/warning)
- 预警统计
- 预警列表
- 预警规则配置

---

## 六、示例追溯码

| 追溯码 | 产品名称 | 状态 |
|--------|----------|------|
| A1B2C3D4E5F6G7H8 | 有机番茄 | 合格 |
| B2C3D4E5F6G7H8I9 | 红富士苹果 | 合格 |
| C3D4E5F6G7H8I9J0 | 东北大米 | 合格 |
| D4E5F6G7H8I9J0K1 | 草莓 | 合格 |
| G7H8I9J0K1L2M3N4 | 葡萄 | 不合格 |

---

## 七、常见问题

### Q1: Docker 启动失败
1. 确保 Windows 已启用 Hyper-V
2. 在 BIOS 中启用虚拟化技术
3. 重启 Docker Desktop

### Q2: 首次启动很慢
首次启动需要：
- 下载 Docker 镜像（约 5GB）
- 构建 Hive 镜像
- 初始化 Hive Schema

后续启动会快很多（约 1-2 分钟）

### Q3: 端口被占用
修改 docker-compose.yml 中的端口映射，或关闭占用端口的程序

### Q4: pip 安装失败
使用国内镜像：
```bash
pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
```

### Q5: 容器启动失败
```bash
# 查看日志
docker logs 容器名

# 重新启动
docker-compose down
docker-compose up -d --build
```

---

## 八、目录结构

```
项目根目录/
├── 启动.bat              # 一键启动
├── 停止.bat              # 一键停止
├── docker-compose.yml    # Docker 编排配置
├── hadoop.env            # Hadoop 环境变量
├── hive/                 # Hive 自定义镜像
│   └── Dockerfile
├── flask_app/            # Web 应用
│   ├── app.py            # Flask 主程序
│   ├── db_service.py     # 数据服务层
│   ├── requirements.txt  # Python 依赖
│   └── templates/        # 前端页面
├── hive_scripts/         # Hive SQL 脚本
├── spark_jobs/           # Spark 分析脚本
├── data/                 # 示例数据
├── 使用说明.md           # 本文档
└── 开发进度.md           # 开发进度
```
